# タスクバー・プラザ（仮）仕様書

## 0. コンセプト概要
- デスクトップのタスクバー上に常駐する、透過ウィンドウの放置系ゲーム
- ストーリーなし。キャラクターが勝手に生活（徘徊・雑談・喧嘩）しているのを眺め、たまに干渉する
- プレイヤーの干渉：話す／モノをあげる／ペチッと叩く
- ゴールは「会話図鑑」を埋めていくこと（コンプリート系のだらだら遊び）
- 参照UX：「学校の教室で友達同士が喋ってるのを聞いてたい」感覚
- エンジン：Godot 4.7

---

## 1. 優先順位リスト（実装フェーズ）

### Phase 0｜技術検証（最優先・最初にやる）
- [x] 透過・ボーダーレス・常に最前面ウィンドウの表示確認
- [x] キャラ1体だけをタスクバー上に表示し、クリックが下のアプリに抜けるか（クリックパススルー）を検証
- [x] Forward+ と Compatibility レンダラーで透過の安定性を比較し、採用レンダラーを決定
- [x] マルチモニタ環境での挙動確認（最低限、メイン画面のみ対応でも可）

→ ここが崩れると企画自体が成立しないため、他の実装より先に着手する。

### Phase 1｜MVP（1体だけで完成させる）
- [x] キャラ1体の徘徊AI（左右移動、端で反転、たまに立ち止まる）
- [x] クリックで「話す」メニューが出て、セリフが1つ表示される
- [x] セリフの重複なしローテーション（3〜5個を順番に）

### Phase 2｜複数キャラ・すれ違いイベント
- [ ] キャラを3体に増やす
- [ ] キャラ同士の距離判定→すれ違いイベント抽選（雑談 or 喧嘩）
- [ ] イベントのクールダウン管理（同じペアが連発しないように）
- [ ] 「話す」以外の干渉（あげる／ペチッ）を追加

### Phase 3｜収集要素（会話図鑑）
- [ ] セリフID・アンロック状態を管理するデータ構造
- [ ] 図鑑UI（未解放は？表示、進捗％表示）
- [ ] セーブ／ロード（ローカルファイル保存）
- [ ] 新規解放時の通知演出（トースト等）

### Phase 4｜仕上げ・拡張
- [ ] キャラ・セリフ・アイテムの追加しやすいデータ駆動設計への整理
- [ ] 見た目調整（アイドルモーション、吹き出しUI、演出強化）
- [ ] 常駐設定（起動時自動起動、タスクトレイ格納など）
- [ ] 設定画面（表示位置、音量、非表示切り替えなど）

---

## 2. 仕様詳細

### 2.1 ウィンドウ表示仕様
| 項目 | 内容 |
|---|---|
| 透過 | ON（Project Settings > Display > Window > Size > Transparent） |
| Per Pixel Transparency | Allowed: ON |
| ボーダーレス | ON |
| 常に最前面 | ON |
| 背景クリアカラー | (0,0,0,0) |
| Viewport Transparent Background | ON |
| クリック透過 | キャラの当たり判定部分のみクリック可能、それ以外は下のウィンドウへ抜ける（`mouse_passthrough_polygon` 相当の仕組みで実現） |
| 配置位置 | 画面下部、実タスクバーに重なる帯状の領域を想定 |
| 対応レンダラー | Phase0の検証結果次第で決定（Forward+で透過が不安定な場合はCompatibilityを採用） |

### 2.2 キャラクター仕様
**状態（ステート）**
- `walk`：徘徊中（デフォルト状態、プレイヤー操作可能）
- `event`：すれ違いイベント中（他キャラと自動会話・喧嘩、操作不可）
- `menu`：プレイヤーがクリックしてメニュー表示中
- `reacting`：話す／あげる／ペチッの反応中（一時的に操作不可）

**徘徊AI**
- 左右移動、画面端（タスクバー領域端）で反転
- 移動速度・待機時間にランダム性を持たせ、キャラごとに個性を出す
- `walk` 状態のときのみ、他キャラとのすれ違い判定・プレイヤークリックを受け付ける

**個体差（初期3体想定）**
| キャラ | 性格 | 備考 |
|---|---|---|
| モモ | 元気・テンション高め | リアクションが大きい |
| ソラ | クール・そっけない | リアクション控えめ |
| ナギ | のんびり・マイペース | 動きがゆったり |

### 2.3 すれ違いイベント仕様
- 2キャラの距離が一定値未満になったら発火判定
- 判定は「未解放セリフを優先」ではなく、既存セリフも含めて等確率抽選（同じ組み合わせを何度も見られる）
- 発火タイプ：雑談（高確率）／喧嘩（低確率）の二択
- 発火中は両キャラとも `event` 状態にし、セリフを順番に吹き出し表示
- 同一ペアには発火後クールダウンを設け、短時間で連発しないようにする
- クールダウン・発火確率は数値を後から調整できるようパラメータ化しておく

### 2.4 プレイヤー干渉仕様
`walk` 状態のキャラをクリックすると干渉メニューを表示。

| 干渉 | 内容 |
|---|---|
| 話す | キャラ固有のセリフをローテーションで表示。図鑑に登録 |
| あげる | アイテム選択（花／おやつ　など）→キャラ反応セリフ表示。図鑑に登録 |
| ペチッ | 軽いリアクション演出（震える・のけぞる等）＋反応セリフ表示。図鑑に登録 |

- 干渉中のキャラは他の干渉・すれ違いイベントの対象にしない
- 干渉終了後、一定時間で `walk` に復帰

### 2.5 会話図鑑（収集要素）仕様
- 全セリフに一意のID・所属キャラ（または「すれ違いイベント」区分）を付与
- 未解放セリフは「？？？」表示、解放済みは全文表示
- カテゴリ分け表示（キャラ別＋すれ違いイベント）
- 進捗はパーセンテージ・カウント（n / 全体数）で常時表示
- 新規解放時は控えめな通知演出（音・アイコン・トーストなど）
- 進捗はセーブファイルに保存し、次回起動時に復元

### 2.6 データ設計（考え方）
- キャラクター定義（ID、名前、外見パラメータ、性格傾向）とセリフDB（ID、所属、テキスト、解放条件種別）を分離
- セリフ・キャラ・アイテムを増やす際にコードを触らず追加できる形（リソースファイル／JSON的な構造）を推奨
- サンプルコードは今回出力しないが、実装時はこの分離を前提に設計する

### 2.7 UI要素一覧
- 吹き出し（会話・雑談・喧嘩共通）
- 干渉メニュー（話す／あげる／ペチッ）
- アイテム選択ミニパネル
- 図鑑パネル（開閉可能、進捗バー付き）
- 新規解放トースト通知

---

## 3. 技術メモ・既知のリスク
- Forward+（Vulkan）では、Alt+Tab後や全画面切り替え時に透過が乱れる不具合報告あり。Phase0で必ず確認する
- 透過＋常に最前面＋最大化の組み合わせで、一部アプリ（ブラウザ等）の描画が固まる不具合報告あり（Windows / 一部バージョン）。長時間稼働を想定するなら注意
- クリック透過の実装（当たり判定に応じた `mouse_passthrough_polygon` 更新）は毎フレーム／物理フレームでの更新が必要になるため、キャラ数が増えたときの負荷は要検証
- 単一ウィンドウは「透明」か「不透明」の二択で、ウィンドウ全体を半透明の帯にすることはできない点に注意（半透明の帯に見せたい場合はキャラ側の描画で表現する）

---

## 4. 今回のスコープ外（初期バージョンでは扱わない）
- 横スクロールでのカメラ移動・広いマップ
- キャラの育成・パラメータ変化
- 課金・アイテム購入要素
- マルチモニタでの複数ウィンドウ同時常駐
- サウンド・BGMの本格実装（効果音程度は任意）
